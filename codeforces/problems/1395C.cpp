#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(int)n;i++)
#define endl '\n'
#define IOS ios_base::sync_with_stdio(0);cin.tie(0)

const int maxn = 205;
int arr[maxn];
int ar[maxn];


void solve(){
    int tp=9;
    while(tp){
    }

}

int32_t main(){
    itn n,m;
    cin>>n>>m;
    rep(i,n)
        cin>>arr[i];    
    rep(i,m)
        cin>>ar[i];
    solve();
    return 0;
}
